# Example 3: New Schema (Working Snapshot) - Empty Tables
# File location: db-schemas/new_project.yml
# Shows a schema that was just initialized with no tables yet.
# Tables can be added incrementally as the schema evolves.

schema:
  name: "New Project"

snapshot:
  label: "v0.0.1"
  tables: []
